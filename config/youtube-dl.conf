# youtube-dl default options
# override with --ignore-config

# extract the audio
--extract-audio
--audio-format mp3

# meta data embedding
--embed-thumbnail
--add-metadata
--xattrs

# default filename
-o "%(title)s.%(ext)s"
